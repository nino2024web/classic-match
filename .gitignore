# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files.
/.env*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets

# Ignore master key for decrypting credentials and more.
/config/master.key

# Secrets
/config/credentials/*.key
# （credentials.yml.enc はコミットする）

# Build
/app/assets/builds/*
!/app/assets/builds/.keep
/public/assets
# Webpacker
/public/packs
/public/packs-test

# Node / JS
/node_modules
yarn-error.log
npm-debug.log*

# Bundler
/vendor/bundle

# エディタ/OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# デバッグ/計測
.byebug_history
.irb_history
.pry_history
/coverage
/spec/examples.txt

# DB_SQLite
/db/*.sqlite3
/db/*.sqlite3-*

# RSpec 由来の一時ファイル
.rspec_status
/spec/system/screenshots
/tmp/capybara

# Rails のキャッシュ類
/db/schema_cache.yml
/tmp/cache/*
!/tmp/cache/.keep

# DB/ミドルのローカルダンプ誤コミット防止
*.sql
*.dump
dump.rdb

# Docker
docker-compose.override.yml

# メールプレビュー
/tmp/letter_opener

# 画像アップロード
/public/uploads
