<div class="relative min-h-screen text-white">
  <picture>
    <source media="(min-width:1024px)" srcset="<%= asset_path('landing/landing-piano-lg.jpg') %>">
    <img src="<%= asset_path('landing/landing-piano-lg.jpg') %>"
         alt="" class="absolute inset-0 w-full h-full object-cover" loading="eager" fetchpriority="high">
  </picture>

  <div class="absolute inset-0 bg-black/40"></div>
  <div class="absolute inset-0 bg-gradient-to-b from-transparent via-black/20 to-black/70"></div>

  <main class="relative z-10 grid min-h-screen grid-rows-[auto_1fr_auto]">
    <header class="flex items-center justify-end px-5 py-4 sm:px-8">
      <%= link_to "ログイン", new_user_session_path,
          class: "rounded-full px-4 py-2 text-sm bg-white/10 hover:bg-white/20 ring-1 ring-white/30 focus:outline-none focus-visible:ring-4 focus-visible:ring-white/40" %>
    </header>

    <section class="px-5 sm:px-10 flex items-center">
      <div class="max-w-[48rem]">
        <h1 class="font-bold leading-tight tracking-tight text-[34px] sm:text-5xl lg:text-6xl text-shadow">
          好きな曲から、<br class="hidden sm:block">つながる。
        </h1>
        <p class="mt-4 sm:mt-6 max-w-prose text-base sm:text-xl text-white/90 text-shadow">
          クラシックの「曲」や「作曲家」をきっかけに、共通の話題から自然に会話がはじまります。
        </p>
      </div>
    </section>

    <footer class="px-5 sm:px-10 pb-safe pb-6">
      <div class="mx-auto w-full max-w-md space-y-3">
        <%= link_to "登録する", new_user_registration_path,
          class: "block w-full text-center rounded-full py-4 font-semibold bg-amber-50/90 text-slate-900 shadow-lg backdrop-blur ring-1 ring-white/40 hover:bg-amber-100 focus:outline-none focus-visible:ring-4 focus-visible:ring-amber-200 transition" %>

        <%= link_to "ログイン", new_user_session_path,
          class: "block w-full text-center rounded-full py-4 font-semibold bg-white/10 text-white shadow-lg backdrop-blur ring-1 ring-white/30 hover:bg-white/20 focus:outline-none focus-visible:ring-4 focus-visible:ring-white/40 transition" %>

        <div class="mt-4 flex flex-col items-center gap-2 text-sm text-white/80 sm:flex-row sm:justify-center">
          <%= link_to "利用規約", terms_path,
              class: "hover:underline focus:outline-none focus-visible:ring-2 focus-visible:ring-white/60 rounded px-1" %>
          <span class="hidden sm:inline">・</span>
          <%= link_to "プライバシー", privacy_path,
              class: "hover:underline focus:outline-none focus-visible:ring-2 focus-visible:ring-white/60 rounded px-1" %>
        </div>
      </div>
    </footer>
  </main>
</div>
